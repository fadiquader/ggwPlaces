import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  inputContainer: {
    // width: '100%',
    padding: 16,
  },
  input: {
    padding: 16,
    borderWidth: 1,
    borderColor: '#ff8456',
    borderRadius: 8,
  }
})
